## Exemplo de variáveis de ambiente
##
## Uso:
## - Copie este arquivo para `.env` (não comite o `.env`)
## - Ajuste os valores

# Backend (obrigatório p/ server)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Frontend (opcional)
# Se NÃO definir, o frontend usa "/api" (dev via proxy do Vite, produção via same-origin).
# Se sua API estiver em outro domínio, defina a URL completa (incluindo "/api"):
# VITE_API_URL=https://seu-dominio.vercel.app/api

# AI (opcional dependendo das features)
# VITE_OPENAI_API_KEY=sk-...



